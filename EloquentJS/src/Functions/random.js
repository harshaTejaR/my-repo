export const random = function (min, max) {
    let apiKey = "ahcsjhvc4343dblvsd";
    return  console.log(`${apiKey} + Random Number ${Math.floor((min + Math.random()*(min-max)))}`) ;
        
    
}